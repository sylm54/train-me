# Generated by Cargo
# will have compiled files and executables
/target/

# Generated by Tauri: schema files for capabilities auto-completion only.
# gen/android IS committed: it carries user-owned Android customizations
# (RenderService.kt, manifest permissions/service) that must survive CI,
# where `tauri android init` otherwise regenerates the project from scratch
# (init preserves committed user files and only refreshes generated/).
# Build outputs and machine-local state stay ignored via the .gitignore
# inside gen/android.
/gen/schemas
